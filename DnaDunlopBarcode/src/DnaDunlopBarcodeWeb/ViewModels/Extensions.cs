using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace DnaDunlopBarcodeWeb.ViewModels
{
    /// <summary>
    /// http://www.extensionmethod.net/csharp/string/defaultifempty
    /// Extension to string to provide default when null
    /// </summary>
    public static class Extensions
    {
        //public static string DefaultIfEmpty(this string str
        //    , string defaultValue
        //    , bool considerWhiteSpaceIsEmpty = false)
        //{
        //    return (considerWhiteSpaceIsEmpty ? string.IsNullOrWhiteSpace(str) : string.IsNullOrEmpty(str)) 
        //        ? defaultValue : str;
        //}
    }
}